# 参与维护

README 面向使用者，介绍用途、接入和更新方式；匹配边界与来源放在 `docs/`，可直接使用的配置示例放在 `examples/`。不要在 README 逐项记录提交历史。

修改规则时，说明需要覆盖的服务、匹配范围和可验证的依据。清单不包含节点、账号、私有域名、订阅令牌或完整订阅 URL。通用规则保持无策略列的文本格式；Surge Mac 进程规则独立维护，不混入跨客户端清单。

避免通过整个云厂商 ASN、宽泛 `openai` 关键词或整个共享云/CDN 后缀扩大 AI 匹配。新增共享登录主机时说明对其他服务的影响，并补充正反例。同步内网清单时审阅上游差异、保留所需条目、去重及保留 `no-resolve`。

在仓库根目录执行：

```sh
python3 -m unittest discover -s tests -v
git diff --check
```

测试检查规则范围、重复项、进程边界和示例对规则文件的引用。它不代替客户端引擎；配置或语法改动还应通过对应客户端的格式检查。规则行为变更需核对实际请求的命中结果，不能只以下载成功判断分流已生效。

新增或重命名 `.list` 文件时，同步 README、示例、使用方的引用和策略选择。现有清单内容合入 `main` 后，由客户端的远程规则更新机制获取；仓库不自动发现使用者的配置，也不替用户修改节点或出口选择。
